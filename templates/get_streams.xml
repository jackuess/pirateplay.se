<streams xmlns:py="http://genshi.edgewall.org/"> 
	<py:for each="s in streams">
		<stream quality="${s['meta'].get('quality')}" subtitles="${s['meta'].get('subtitles')}">${s['url']}</stream>
	</py:for>
</streams>